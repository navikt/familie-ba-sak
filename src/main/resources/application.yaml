server:
  servlet:
    context-path: /
  port: 8089

spring:
  autoconfigure.exclude: org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration
  main.banner-mode: "off"
  datasource:
    hikari:
      maximum-pool-size: 2
      connection-test-query: "select 1"
      max-lifetime: 30000
      minimum-idle: 1
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQL95Dialect"
        temp:
          use_jdbc_metadata_defaults: false
  flyway:
    enabled: true

logging:
  config: "classpath:logback-spring.xml"

funksjonsbrytere:
  enabled: true
  unleash:
    uri: https://unleash.nais.adeo.no/api/
    environment: ${NAIS_CLUSTER_NAME}
    applicationName: ${NAIS_APP_NAME}

management:
  endpoint.health.show-details: always
  endpoints.web:
    exposure.include: info, health, metrics, prometheus
    base-path: "/internal"
  metrics.export.prometheus.enabled: true
  health:
    db:
      enabled: true
  metrics:
    web:
      server:
        request:
          autotime:
            enabled: true

BA_SAK_FRONTEND_CLIENT_ID: "dummy"
BA_MOTTAK_CLIENT_ID: "dummy"
FAMILIE_PROSESSERING_CLIENT_ID: "dummy"
FAMILIE_OPPDRAG_API_URL: http://localhost:8087/api
FAMILIE_BA_DOKGEN_API_URL: http://localhost:8080